package com.example.composeuicomponents.ui.theme.dimensions

import androidx.compose.foundation.shape.CornerSize
import androidx.compose.ui.unit.dp

val BORDER_RADIUS_NONE = CornerSize(DIMEN_NONE)
val BORDER_RADIUS_SM = CornerSize(DIMEN_SM)
val BORDER_RADIUS_DEFAULT = CornerSize(DIMEN_DEFAULT)
val BORDER_RADIUS_MD = CornerSize(DIMEN_MD)
val BORDER_RADIUS_LG = CornerSize(DIMEN_LG)
val BORDER_RADIUS_XL = CornerSize(DIMEN_XL)
val BORDER_RADIUS_2XL = CornerSize(DIMEN_2XL)
val BORDER_RADIUS_3XL = CornerSize(DIMEN_3XL)
val BORDER_RADIUS_FULL = CornerSize(999.dp)
